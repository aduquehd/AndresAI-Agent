{
  "name": "ai-agent",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build:js && npm run build:css",
    "build:js": "tsc && npm run minify:js",
    "minify:js": "find static/js -name '*.js' -type f | while read file; do npx esbuild \"$file\" --minify --sourcemap --target=es2020 --format=esm --outfile=\"$file\" --allow-overwrite; done",
    "build:css": "cp -r frontend/src/css/* static/css/ && npm run minify:css",
    "minify:css": "find static/css -name '*.css' -type f | while read file; do postcss \"$file\" -o \"$file\" --use cssnano --no-map; done",
    "watch": "tsc --watch",
    "dev": "npm run watch",
    "copy-css": "cp -r frontend/src/css/* static/css/",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:html": "prettier --write \"templates/**/*.html\"",
    "format:js": "prettier --write \"static/**/*.js\"",
    "format:css": "prettier --write \"static/**/*.css\""
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "cssnano": "^7.1.0",
    "esbuild": "^0.24.2",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
